# 2026-07-04 工作日志

## fix: SAP BOM 位号延续行解析 (3137817)

### 问题
用户发现组件导出 Excel 中 N010702-000076-001 用量=96 但位号只显示 6 个（C0S18 C0S21 C0S23 C0S27 C0S28 C0U12）。

### 根因
SAP BOM 展开表中，位号超过一行时系统会以**延续行**形式在后续行追加（每行 6 个位号），中间可能跨页。旧版 `bom_cleaner.py` 的 `extract_data_row()` 只解析数据行的 col[25:]，不处理延续行，导致大量位号丢失。

如 N010702-000076-001 的 96 个位号分布在 17 行延续行中（含跨页），旧版只取了第一行的 6 个。

### 修复
- 新增 `_is_ref_continuation_line()` 检测位号延续行（非表头/标题/数据行，含非空字段）
- 新增 `_collect_continuation_refs()` 从数据行后跨页收集所有延续位号
- `parse_bom_clean` / `clean_bom_data` 主循环改为索引遍历，数据行后调用收集函数

### 效果
| PN | 修正前 ref_count | 修正后 ref_count |
|---|---|---|
| N010702-000076-001 (qty=96) | 6 | **96** |
| N010904-000004-001 (qty=53) | 6 | **53** |
| N010702-000179-001 (qty=69) | 6 | **69** |
| N010904-000136-001 (qty=102) | 6 | **102** |

### 部署
- PID 837212 → 2778054
- ⚠️ 需重新导入 P1C100A7HP8T702000 才能得到完整位号数据（旧 DB 数据已截断）

## feat: upload 选择文件后自动上传 (a4e32e7)
- selectFile() 末尾调用 uploadFile()，选完文件直接上传
- 按钮改为 secondary + "重新上传"标签，作为手动兜底
- 部署: PID 2778054 → 2786494

## feat: compare 组件树默认全展开 (356ca1e)
- renderComponentTree 中 defaultExpand 逻辑从 false → true
- 加载时所有子件节点默认展开，不用手动逐层点
- 部署: PID 2786494 → 2795837

## refactor: compare 组件树只展开到叶子父组件 (9eea099)
- defaultExpand 改为「仅当子节点中有孙节点时才展开」
- 叶子物料层默认折叠，只显示到叶子数据的直接父组件
- L1→展开, L2(叶子父)→折叠, L3(叶子)→无子节点
- 部署: PID 2795837 → 2799835

## feat: compare 混合节点只显示父组件 (50d5987)
- 当组件同时包含子父组件和叶子物料时（混合节点），展开只渲染父组件
- 叶子物料合并为汇总行 '··· +N 个底层物料'
- 纯父组件节点 / 纯叶子节点行为不变
- 部署: PID 2799835 → 2817870

## fix: 手动展开节点时其他组件全折叠 (b93d0b3)
- 根因: 点击 ▸ 后 expandedSet 非空 → defaultExpand=false → 其他父节点坍塌
- 修复: 展开判断从 `if (defaultExpand && hasKids)` 改为 `if (hasKids && !(pn in expandedSet))`
- 用户未操作的节点仍然走默认展开逻辑

## feat: compare 组件树差异筛选开关 (4e4eaa8)
- 新增全局药丸开关: 原始状态(全部显示) ↔ 差异状态(仅显有差异组件)
- buildDiffParentPNs() 从比对结果提取差异父组件，递归标记祖先
- hasDiffDescendant() 带缓存快速判断
- renderNodes 差异模式隐藏无差异节点(CSS过渡动画)
- 底部汇总行 'N 个组件无差异已隐藏'
- 比对前开关灰色禁用 → loadResult 后启用
- 部署: PID 2968732 → 3147321 (需 Ctrl+F5 刷新)

## refactor: 删除「导出返工变更单」功能 (7a4d66b)
- 实际工作中用不到，彻底清理：
  - compare.html: 删除返工按钮HTML、rework-bar切换区域、reworkMode变量、exportReworkReport/toggleReworkMode函数、REWORK_TYPE_MAP
  - report.py: 删除 `/api/export-rework/<int:task_id>` 路由
  - reporter.py: 删除 `generate_rework_order_excel()` 和 `_write_rework_section()` (~180行)
- 共删除 249 行，新增 1 行（保留 NORMAL_TYPE_MAP 供 renderResults 使用）

## refactor: 删除「导出 HTML 报告」功能 (e992210)
- compare.html: 删除按钮HTML + exportHtmlReport() JS函数
- compare.py: 删除 /api/html-report 路由 + _extract_core() + _extract_model()
- html_report.html: 删除整个 Jinja2 模板文件
- 共删除 299 行（3 文件变更）
- 服务器部署: scp 同步 compare.html + compare.py, rm html_report.html, run.sh --force 重启 (PID=3032332)

## refactor: 统一命名「BOM差异分析工具」(41e73a6)
- 全站 title/meta/navbar/footer/页面标题统一改名
- 4 文件: base.html, bom_list.html, index.html, sop.html
- 部署: scp 同步 4 模板, PID 3032332 → 3139044

## feat: SAP BOM 窄列格式兼容 (90112f0)
- 问题: 模组BOM（N012301-015314-001）列位偏移-2，解析报错 float('A优选')
- 新增 detect_sap_layout(): 自动检测宽/窄格式（扫描前20行，按col[15]/col[17]数值分布投票）
- extract_data_row(cols, offset): offset=-2 时 qty→col[15], prio→col[17], ecn→col[20], unit→col[22], ref→col[23:]
- is_title_row(): 描述字段兼容 col[9] 或 col[10]
- 回归测试: 宽列 711行6层级 ✅ | 窄列 31行3层级 ✅
- 部署: scp bom_cleaner.py, PID 3139044 → 3297674

## fix: 窄列格式物料名称全空 (1f1a145)
- 根因: extract_data_row 硬编码 col[10] 读名称，窄列格式描述在 col[9]
- 修复: `get(10, '') or get(9, '')` 兼容两种格式
- 部署: scp bom_cleaner.py, PID 3297674 → 3323613
