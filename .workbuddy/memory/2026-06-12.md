# 2026-06-12 工作日志

## BOM 比对工具启动修复

**问题**：启动 `.bat` 报错 `SyntaxError: unexpected character after line continuation character`，`compare.py` 第1行 `from flask import ...\nimport os` 存在字面 `\n` 字符。

## H5F→P3EM 父节点合并 (17:38-17:48)

**需求**：WORD 导出只展示 P3EM 父节点，H5F 父节点隐藏，但 H5F 叶子数据完整保留并归入对应 P3EM 父节点下。

**实现** (`change_notice.py`):
- 新增 `_get_functional_key()` 提取组件功能前缀（如 "液晶模组"、"电子BOM"、"包装组件"）
- 重写 `group_diffs_by_parent()`:
  1. 分组后拆分为 P3EM (ADD-only) / H5F (DEL-only) / MOD 三类
  2. 建立 P3EM 功能键索引
  3. H5F 组按功能键匹配合并到 P3EM 组
  4. 合并时丢弃 H5F 父节点自引用项
  5. 仅对 P3EM 和 MOD 组的父节点做 self-referencing

**验证结果**: 16组→9组, 3个H5F纯父节点隐藏(N030103-024810-103, N030103-024817-003, N030105-025860-001), 0缺失/0多余, 7对H5F→P3EM全部成功匹配

## 按钮尺寸统一修复

- **app.css**: `.btn` 基类增加 `min-height: 36px`、`font-family: inherit`、`line-height: 1.25rem`、`box-sizing: border-box`
- `.btn--sm`: 增加 `min-height: 28px`
- `.btn--lg`: 增加 `min-height: 44px`
- 新增 `.btn--orange`（返工变更单 #f97316）和 `.btn--indigo`（Word通知单 #1e40af）颜色类
- **bom_list.html**: 移除3个头部按钮的 `text-sm` Tailwind 类，统一使用 `.btn` 基类控制尺寸
- **upload.html**: 移除导航按钮 `text-sm` 类
- **compare.html**: 3个导出按钮改用新颜色类替代 inline style

## BOM 逻辑图导出位置分析

分析结论：推荐在 **bom_list.html**（BOM管理页）的操作列添加"逻辑图"按钮。
理由：该页是BOM管理的核心页面，已有「导出 | 组件 | 删除」操作列，新增「逻辑图」按钮（导出 → 组件 → 逻辑图 → 删除）符合用户心智模型。
首页是总览入口不适用，上传页聚焦上传非浏览，比对页侧重差异分析且已有组件树。
交互方式：点击按钮 → 弹出模态框展示层级树结构 → 内嵌"导出PNG/SVG"按钮。

**根因**：`compare.py` 在之前的写入操作中换行符被错误转义为字面 `\n`（共176处）。`change_notice.py` 及其他文件的大量 `\n` 计数实为字符串内的合法转义序列，非损坏。

**修复步骤**：
1. `git checkout HEAD -- app/routes/compare.py` — 恢复干净版本
2. 补充 `import os` 和 `/api/change-notice/<int:task_id>` 路由
3. `pip install python-docx` — venv 中缺少该依赖

**验证**：`create_app()` 成功，`change_notice` 模块正常导入，路由 `/compare/api/change-notice/<int:task_id>` 已注册。

## 重构 Word 更改通知单生成（模板填充替代裸 XML 构建）

**背景**：之前导出文档从零构建表格，与官方模板存在6项差异（丢失阶段/数量行、拟制/审核行、字号偏小、编号错误等）。

**重构方案**：改为加载模板 `整机清机更改通知单.docx` 并填充值单元格：
- 新增 `_fill_template(table, ...)` — 按模板结构填值
- 新增 `_set_cell_text(cell, text)` — 按模板样式（宋体14pt）写值
- 新增 `_fmt_qty(q)` — 数量整数格式化
- 删除废弃函数：`_oxml`, `_make_cell_props`, `_make_para`, `_make_tc`, `_make_tr`, `_make_simple_cell`, `_build_full_table`, `_build_diff_header_row`, `_build_diff_data_row`

**结果**：生成文档与模板结构完全一致（6行×10列），所有缺失行恢复，字号匹配，编号/说明文字正确。

## 添加 Word 更改通知单前端导出按钮

**内容**：
1. `compare.py` 新增 `GET /compare/api/download-change-notice/<task_id>` 路由，生成 DOCX 后以 `send_file` 附件形式返回
2. `compare.html` 在结果区添加「导出 Word 更改通知单」按钮（蓝色），调用 `/api/change-notice/{id}` 触发生成后跳转下载
3. 新增 `exportChangeNotice()` JS 函数，含 loading 态与错误处理

**注意**：需用户重启 BOM 工具（kill旧进程 + 重新双击 `.bat`）使新路由生效。

## 修复组件排序（parent_pn 恢复）

**问题**：导出 DOCX 中组件排列顺序与之前确认版本不一致。数据内容一致（21行、5组件），但排序有变。

**根因**：`group_diffs_by_parent()` 第275行排序 key 为 `short_name`（拼音排序），而确认版本使用 `parent_pn` 排序。

**修复**：`return sorted(..., key=lambda g: g.get('parent_pn', ''))`。

**提交**：`863c465` → 已推送到 origin/main。

**补充修复**（`9630a9a`）：发现部分 diff 行 parent_pn 在目标BOM中以X后缀存储（如 `N030105-025518-001X`），导致 parent_lookup 命中失败，parent_name 为空。`group_diffs_by_parent` 合并后可能产出空 parent_pn 组。修复：
1. `group_diffs_by_parent` 最终返回前过滤 `parent_pn` 为空或 `__UNKNOWN__` 的组
2. `_fill_template` 组件标题处增加 `not comp_pn` 防御检查
验证：task_id=45 正确输出5个组件组，按 parent_pn 排序。

**根因确认 — Flask 旧进程缓存**：用户导出空白 DOCX，经端到端验证：
- 直接调用函数：✅ 21段、5组、数据完整
- Flask HTTP API（旧进程）：❌ 0段、文件仅模板结构
- Flask HTTP API（新进程 + 清缓存后）：✅ 21段、5组、数据完整

**根因**：端口 5002 上残留 3 个旧 gunicorn worker 进程（PID 22484/23544/18884），加载了中间态 `.pyc`（仅有 parent_pn 排序修复但无空组过滤），导致 `group_diffs_by_parent` 误过滤全部组。清理步骤：
1. PowerShell `Stop-Process` 强杀全部 Python 进程
2. 清理 `__pycache__` 目录和 `.pyc` 文件
3. 重启 Flask 服务

**二次根因 — .bat 端口清理漏洞**（`ab3d276`）：用户重启后仍空白。复盘发现 `.bat` Step0 的 `for /f tokens=5` + `netstat` 方式仅捕获到 2 个 PID（1816/8780），漏杀第 3 个旧进程（PID 20096）。旧进程继续监听 5002 端口并处理请求。

**修复**：`.bat` Step0 改为：
- `taskkill /F /IM python.exe` 强杀全部 Python 进程
- `rmdir /s /q __pycache__` 递归清理所有 `.pyc` 缓存
- 等待端口释放后再启动 Flask

**终极根因 — differ.py 从未写入 parent_pn**（`192d3ac`）：用户再重启后导出仍空白。彻查发现根本原因在 **BOM 比较引擎**，与缓存/进程无关。

| 检查项 | 结果 |
|--------|------|
| 表结构 | `comparison_result` 有 `parent_pn_a`/`parent_pn_b` 列（TEXT） |
| Task #45 | parent_pn 填充 16/30 ✅（Git 恢复前旧代码版本产物） |
| Task #46-54 | parent_pn **全部为空** ❌ |
| differ.py INSERT | 从未包含 `parent_pn_a` 和 `parent_pn_b` 字段 ❌ |
| change_notice.py | 正确过滤空 parent_pn 组 → 全被过滤 → 空白 DOCX |

**修复**：
1. `differ.py`：三个 diff 步骤（added/removed/modified）均从 `item_b`/`item_a` 获取 `parent_pn`，存入 diff_record
2. `differ.py` INSERT 新增 `parent_pn_a`, `parent_pn_b` 两列（共 18 个参数）
3. 数据库回填：Tasks 46-54 从 `bom_item` 回填 parent_pn（每 task 回填约 30 行）

**验证**：Task #54（回填后）、Task #55（新建）均生成 21 段落、5 组件组、13586 字节的正确 DOCX。

## 统一 BOM 对比术语（`4fc60e3`）

**背景**："来源BOM（基准）" / "目标BOM（对比）" 存在歧义——"来源"字面可误解为变更来源（新BOM），"目标"可误解为目的地（旧BOM），且组件面板用第三套命名"主BOM/副BOM"。

**修改**（纯文本，不影响逻辑）：
- `compare.html`：下拉框 `来源BOM（基准）`→`基准BOM`、`目标BOM（对比）`→`目标BOM`；组件面板 `主BOM组件`→`基准BOM组件`、`副BOM组件`→`目标BOM组件`；Toast警告同步更新
- `index.html`：仪表盘表头 `来源BOM`→`基准BOM`
- `upload.html`：批量导出标签 `来源BOM`→`基准BOM`
- `bom_list.html` 中 SAP BOM类型徽章（主BOM/副BOM=primary/alternative）不受影响

## BOM 元数据5字段新增（`b77d134`）

**需求**：首页BOM清单列表新增5个元数据字段，从上传文件自动解析，仅展示不导入bom_item。

**新增字段**：有效日期从(valid_from)、顶层BOM号(bom_number)、最后更改号(ecn)、BOM状态(bom_status)、BOM工厂(bom_plant)

**修改文件**：
- `bom.py`：bom_header表新增5列（CREATE TABLE + ALTER TABLE迁移）
- `config.py`：COLUMN_MAP新增5组候选列名（中英文混合）
- `parser.py`：SAP分支从bom_cleaner metadata取值；标准分支从DataFrame列提取首行非空值；新增`_normalize_date()`兼容6种日期格式；`get_uploaded_boms()`返回新字段
- `app.js`：新增`formatDateOnly()`和`getStatusBadge()`（4色标签：绿=生效、红=失效、橙=待审核、蓝=其他、灰=暂无数据）
- `index.html`：首页概览表从5列扩展到10列
- `bom_list.html`：管理页表从9列扩展到14列，colspan同步更新

**状态标签映射**：生效/激活→绿色、失效/废弃→红色、待审核/冻结→橙色、其他→蓝色、空→灰色"暂无数据"

**验证**：API正常返回新字段，日期标准化全格式通过，Flask重启后页面正常渲染

## 比对类型差异化完善（`efba326`）

**背景**：版本比对和跨机型比对之前仅有标签差异，实际比对逻辑完全相同。

**修改**：
- `differ.py`：
  - **版本比对** (Step 3b)：同 PN 同父件但版本号不同 → 标记为 MOD (diff_category='version', severity='low')
  - **跨机型比对** (Step 3c)：同 PN 但父件不同 → 标记为 MOD (diff_category='structure', severity='medium')
  - task_name 增加 `[版本比对]` / `[跨机型比对]` 前缀
  - `_classify_severity()` 新增 version→low, structure→medium
- `reporter.py`：TYPE_LABELS 新增 version/跨机型比对→中文映射，报告中"比对类型"字段同步显示中文

## bom_list 字段调整（`230feed`）

**修改**：
- `bom_list.html`：
  - 列标题重命名：`BOM 名称`→`上传文件名`、`有效日期从`→`BOM激活日期`
  - 移除 `版本` 列（表头 + 数据行），colspan 从 14→13
  - 数据列改用 `source_file || bom_name` 回退显示
  - 搜索逻辑：移除版本字段搜索，改为搜索 `source_file`
  - 双导出下拉 + 删除确认同步使用 `source_file || bom_name`
- `app.css`：`.data-table td` 新增 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 220px` 禁用自动换行

**跨引用排查**：`compare.html`、`upload.html`、`reporter.py`、`differ.py`、`change_notice.py` 中使用的 `bom_name`/`bom_version` 均为数据属性，非 bom_list 表格列引用，不受影响

## 完全移除"严重度"概念

用户要求移除所有"严重度"相关内容。改动覆盖前端和后端共 7 个文件：

| 文件 | 改动 |
|------|------|
| `compare.html` | 移除严重度筛选器（3个checkbox + 标签）、表格严重度列、SEVERITY_MAP变量、行渲染中severity徽章/着色、applyAllFilters中severity过滤、resetFilters中severity重置；colspan 9→8；行边框颜色改为按diff_type着色 |
| `differ.py` | 删除 `_classify_severity()` 函数；移除5处 diff_records 中 `'severity'` 赋值；DB insert 保留 `rec.get('severity', 'medium')`（向后兼容） |
| `reporter.py` | 删除 SEVERITY_STYLES/SEVERITY_LABELS；stats查询移除severity分组；汇总移除"严重度"标签；明细表头移除"严重度"列、行数据移除severity_cn、填充色改为无填充 |
| `compare.py` | ORDER BY 从 `severity DESC` → `diff_type` |
| `report.py` | stats查询移除severity字段 |
| `index.html` | `severityBadge` 变量重命名 → `diffBadge`（不影响功能） |
| `app.css` | 注释"Row severity left border" → "Row type left border"（行边框样式保留，改按 diff_type 着色） |

**保留未改**：`bom.py:86` severity列定义 + `differ.py:342,350` DB写入 — 都是向后兼容的数据库层，不暴露给用户。

## Word 导出分页修复验证

通过 Python 脚本全面验证生成的 DOCX（100H5F vs 100P3EM）：
- **分页符**: 16 个 `WD_BREAK.PAGE`，产生 17 页内容分区 — 第1页为表头信息，第2-17页为16个组件组各自独立分页
- **组件组检测**: 16 个 "在XXX……XXX里" 标题，每个前都有分页符
- **格式保留**: 字体 (宋体, 楷体_GB2312) / 字号 (12pt/18pt 等) / 颜色 (蓝色 #1E40AF 表头, 深灰 #334155 正文) / 粗体标记 18 处 — 全部保留
- **表格**: 1 个主表格 6 行，结构完整
- **结论**: ✅ 所有检测项通过

## BOM 逻辑图导出功能实现

**功能**：在 bom_list.html 操作列新增"逻辑图"按钮，点击弹出全屏模态框展示 SVG 层级树结构图，支持：
- 导出 SVG 矢量图
- 导出 PNG 位图（2x retina）
- 鼠标拖拽平移 + 滚轮缩放
- 缩放比例控制（− / + 按钮 + 百分比显示 + 重置）

**实现细节**：

| 文件 | 改动 |
|------|------|
| `bom_list.html` | 操作列新增 `<button onclick="openLogicDiagram(id)">逻辑图</button>`（紫色）；新增 `#logic-diagram-overlay` 模态框（header含BOM名/导出按钮、zoom bar、viewport画布、footer节点数） |
| `bom_list.html` `<script>` | 新增 `logicDiagram` 状态对象 + `openLogicDiagram()`/`closeLogicDiagram()` + `buildTreeSVG()` 递归树布局算法（Pass1 计算子树高度、Pass2 分配位置）+ `fitDiagramToViewport()`/`applyDiagramTransform()` 缩放适配 + `downloadDiagramSVG()`/`downloadDiagramPNG()` 导出 |
| `app.css` | 新增 `.logic-diagram-modal` / `__header` / `__title-group` / `__bom-name` / `__actions` / `__zoom-bar` / `__zoom-level` / `__viewport` (网格背景+cursor:grab) / `__canvas` (transform动画) / `__loading` / `__footer`；新增 `.text-indigo-600` / `.text-indigo-800` |

**树图渲染**：
- 每个节点 = 210×60 圆角矩形，按层级着色（6色渐变：黄→蓝→绿→紫→粉→青）
- 节点内显示：层级徽章(圆圈) + PN(等宽字体) + 名称 + 数量×单位(右下角) + 子件数(右上角)
- 连线 = 贝塞尔曲线 SVG `<path>` + 箭头
- 布局 = 两遍递归算法（与 D3 tree layout 同等效果）
- 使用 `createElementNS` 构建 SVG DOM，兼容性好

**API 依赖**：复用现有 `/upload/api/bom-tree/<bom_id>` 端点（返回 `{tree: [...], total_items: N}`）

**验证**：API 测试通过（BOM 1: 676 items, 4 roots），页面渲染正常

## 比对差异变化根因分析

**问题**：上午核对 100H5FP vs 100P3EM 差异约 30 条，下午变为 135 条，用户怀疑规则逻辑变更导致。

**根因**：commit `efba326` (11:32) "完善比对类型差异化逻辑" 在 `differ.py` 中新增 Step 3c 结构变更检测。

**旧逻辑**（上午）：
- Step 3 中同 PN 但不同父件的物料 → 直接跳过（注释: `Same PN under different parent — skip quantity check`）
- 结果：15 added + 14 removed + 1 quantity = ≈30 条

**新逻辑**（下午）：
- Step 3c 显式检测同 PN 不同父件的结构变更，报告为 `diff_category='structure'`, `severity='medium'`
- 结果：105 structure + 15 added + 14 removed + 1 quantity = 135 条

**关键代码变更**：`differ.py` 第 255-326 行新增 72 行（Step 3b 版本检测 + Step 3c 结构检测），added/removed/quantity 原有逻辑未变。

**结论**：差异增长来自新增的结构变更检测（105条），不是原有逻辑出错。上午的 added/removed/quantity 三类数据保持一致。

## structure 分类完善

- **分析结论**: `structure` 分类（同PN不同父件）有独立业务价值（影响产线物流和装配工位），决定保留并完善
- **改动**:
  - `compare.html`: CAT_MAP 补全 `structure: '结构变更'`, `version: '版本变更'`；新增"结构变更"筛选 radio；`getFilterCategory` 支持 structure 独立过滤；badge 按分类着色
  - `app.css`: 新增 `.badge--purple` 样式（紫底紫字）
- 现在 structure 分类可被用户独立筛选和识别，不再混入 other 桶

## Word 分页彻底修复（第二版）

- **问题复现**: 用户下载 docx 后仍无分页显示。前版修复用 `WD_BREAK.PAGE` + `w:pageBreakBefore`，均写入成功但**在合并表格单元格内不生效**——Word/WPS 将单元格视为连续流，分页符仅作软分隔
- **方案**: 将组件组内容从表格单元格移至**文档正文级别**（`doc.add_page_break()` + `doc.add_paragraph()`）——这是唯一所有 Word 处理器都正确渲染的方式
- **改动**:
  - `change_notice.py`: `_fill_template()` 拆分为 `_fill_template_header()`（仅填表头行0-2）+ `_build_content_body(doc, groups)`（文档级段落+分页）
  - `generate_change_notice()` 调用流程相应调整：`_fill_template_header()` → `_build_content_body()`
  - 删除 `_apply_page_break_before()` 和 `from docx.oxml.ns import qn`
- **验证**: 新文档含 15 个文档级分页符（16 组），0 个单元格内分页符

## Word 导出数据逐项核对 (task_id=9, 100H5F vs 100P3EM)

### 通过项
- **类型分类(ADD/DEL/MOD)**: 全部匹配 DB，无错误
- **数量数据**: 唯一 quantity 变更 N010904-000589-001 (17→13) 正确
- **表头字段**: 机芯/机型/阶段/日期/订单号/数量 数据正确
- **分页**: 15 个文档级分页符 ≥ 14（15组-1），每组独占一页
- **空值/重复**: 0 条空值，0 条重复

### 差异项
| 问题 | 数量 | 严重度 |
|------|------|--------|
| 7 条 added/material 中间组件缺失 | 7 | **高** — 数据不完整 |
| 父件分组错误 (merge-by-short-name) | 11 个条目 | **中** — 组标题有歧义 |

### 根因分析
1. **7 条缺失**: `group_diffs_by_parent()` 过滤逻辑（L236-242）跳过"所有子项都是中间组件"的父件组，但这些 added 的中间组件本身是 BOM-level 新增物料，不应被过滤
2. **11 条分组错误**: merge-by-short-name（L248-273）将同类型组件合并（如 N030103-025228-001 整机非公共组件 → N030103-025228-002），导致 DEL 条目出现在错误的组名下

### 修复应用
- **Fix 1 (group_diffs_by_parent)**: 过滤条件从 `all(item['pn'] in all_parents)` 改为 `all(item['type']=='modified' and item['pn'] in all_parents)`，added/removed 类型的中间组件不再被跳过
- **Fix 2**: 移除整个 merge-by-short-name 块（L246-273），直接按 parent_pn 分组，不合并
- **验证结果**: DB 135 条 = Word 135 条，0 缺失，0 分组错误，0 数量错误，26 组 25 个分页符，全部通过

## 移除 structure 差异类型（业务需求）

- **背景**: 用户指出跨机型比对的核心目标是确认叶子物料差异，而非 BOM 层级结构。structure 变更（同PN不同父件）不影响最终叶子物料集合，属于噪音，应移除。
- **数据佐证**: 当前 100H5F vs 100P3EM 共 135 条差异中，105 条（78%）为 structure 类型，且与 material/quantity 完全无重叠。
- **改动**:
  - `differ.py`: 删除 Step 3c（L272-305 structure 变更检测）；更新 docstring 说明结构变更不报告的原因
  - `compare.html`: 移除 structure 筛选 radio；CAT_MAP 移除 structure 条目；badge 移除 `badge--purple` 引用；`getFilterCategory` 移除 structure 分支
  - `app.css`: 删除 `.badge--purple` 样式
- **影响**: 后续跨机型比对仅输出 material（added/removed）和 quantity 差异，差异数量将大幅减少但全为实质性差异。
- **未改**: `change_notice.py`（无 structure 数据后自然不展示）、`reporter.py`（同）、DB 中已有的 structure 历史数据保留不删。

## structure 移除后 Word 导出全面验证

- **验证文件**: `整机清机更改通知单_100H5F_vs_100P3EM_20260612_153811.docx` (Task #13, 30条差异)
- **验证脚本**: `verify_docx.py` — 6大维度自动化检查

| 维度 | 结果 | 详情 |
|------|------|------|
| 数据完整性 | ✅ | DB 30 = Word 30, 0缺失, 0多余 |
| 内容准确性 | ✅ | 18条前缀+12条续行(设计), 数量全匹配 |
| 格式规范性 | ✅ | 宋体, 22pt标题/11pt组标题/10pt正文, 蓝(#1E40AF)+深灰(#334155) |
| 表头 | ✅ | 机芯8R713/机型100P3EM/阶段DVT/日期2026.6.12/订单2606002KL/数量1 |
| 分页 | ✅ | 17个文档级分页符, 16个组件组各占一页 |
| 异常场景 | ✅ | 无乱码, 最长86字符, 特殊字符(Ω±→·…)正常, 名称截断22条属clean_material_name预期 |

- **类型标记设计**: 每组同类型物料仅首行显示ADD/DEL前缀, 续行以5空格缩进
- **名称截断**: clean_material_name() 保留核心描述(如"面壳组件 100H5F 磨砂黑"), 省略工厂/品牌后缀
- **structure移除生效**: Task #13 仅含 material(29) + quantity(1), 0条 structure ✅

## Word 导出分页优化

- **问题**: 之前每组前面强制 `doc.add_page_break()`，30条数据 16 组 → 17 个分页符（每组独占一页，大量空白）
- **方案**: 改为按内容填充分页——先计算每组等效行数（1标题 + N物料 + 1空白），累计行数超过页面预算(70行/页，首页开销20行)时才插入分页符
- **实现**: `_build_content_body()` 重构，提取 `_add_group_header()` / `_add_item_line()` / `_add_spacer()` 辅助函数，消除重复代码
- **效果**: 17 分页符 → 1 分页符（首次溢出时），17 页 → 2 页（54 等效行 = 首页 50 行 + 次页 4 行）
- **验证**: 30/30 数据完整 ✅，名称/数量/格式全部 PASS ✅
- **涉及文件**: `change_notice.py` (_build_content_body 重构), `verify_docx.py` (分页检查适配新规则)

## L1 层级组件过滤 + Self-referencing 修复

- **问题**: 糖宝导出 (103428) 包含 L1 层级组件数据，与上午正确版本排版不一致
- **根因分析** (3个文档对比):
  - 094542 (09:45 上午正确): 扁平列表, 30条全部, 表格式, 无分组 — 基准
  - 103428 (10:34 糖宝): 旧合并表格式, 仅16条, 缺14条, merge-by-short-name bug 残余
  - 153811 (15:38 下午): 文档级分段, 30条完整, 但含 P1C* L1 层级冗余群组
- **修复1 — P1C* 过滤**: `group_diffs_by_parent()` 排除 parent_pn 以 P1C 开头的组（顶级整机编码）。这些组的 item 已在其他组中作为 group header 展示，属冗余 BOM 层级暴露。
- **修复2 — Self-referencing**: 过滤 P1C* 后，6 个既是 group header 又是 diff item 的 PN (电子BOM/大配管BOM/结构BOM) 无法出现。新增逻辑：在 `group_diffs_by_parent()` 中，对 parent_pn 存在于 diff_rows 中的组，在 add/del 列表首位插入 self_item。
- **效果**: 16 组, 38 行 (16 prefixed + 22 continuation), 30/30 唯一 PN, 0 P1C ✅, 数据完整
- **涉及文件**: `change_notice.py` (group_diffs_by_parent 新增 P1C 过滤 + self-referencing 逻辑), `verify_docx.py` (更新路径)
