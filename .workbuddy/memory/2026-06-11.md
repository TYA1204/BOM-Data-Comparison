# 2026-06-11 工作日志

## 比对引擎重构 + 树形展示 + 返工变更单 (commit 78dc1a7)

### 后端变更

**differ.py — 比对引擎简化**
- 移除 `matcher.py` 模糊匹配依赖，改用 `_build_exact_index()` 精确PN匹配
- 简化为3维对比: 新增物料(added) / 删除物料(removed) / 用量变更(quantity)
- 移除的功能: 位号变化检测、物料整合检测、层级/父件变更检测、规格/版本/MPN对比
- 新增参数:
  - `exclude_parents` — 递归排除指定PN及其所有子孙节点
  - `exclude_leaves` — 仅对比有子件的成品模块（排除叶子节点）
  - `skip_pns` — 结果后过滤，排除指定PN
- 严重度简化为 high(新增/删除) 和 medium(用量变更) 两级

**upload.py — BOM树API**
- 新增 `/api/bom-tree/<bom_id>` 返回完整层级树JSON
- 递归构建 children 嵌套结构，含 level/line_no/children_count

**report.py + reporter.py — 返工变更单**
- 新增 `/api/export-rework/<int:task_id>` 路由
- `generate_rework_order_excel()` 生成4个Sheet:
  - 返工概览（汇总信息）
  - 需拆除清单（红色，#FFCCCC）
  - 需补装清单（绿色，#C6EFCE）
  - 用量调整清单（橙色，#FFE5CC）

### 前端变更

**compare.html**
- 新增「全量对比」模式 radio，选择后面板变灰(opacity:0.35)
- 新增「仅对比成品模块」复选框 (exclude_leaves)
- 新增「排除物料号(含子件)」文本域 (exclude_parents)
- BOM组件面板从表格式改为树形展示:
  - 可展开/折叠节点
  - 层级缩进(depth × 18px)
  - 深度着色(0-6级各不同背景色)
  - renderComponentTree() / expandTreeNode() / collapseTreeNode()
  - 搜索时自动展开匹配节点的祖先
- 副BOM树按主BOM顺序自动排序(sortTreeBySource)
  - PN精确匹配 → 名称精确匹配 → 名称核心前缀匹配 → 全局顺序回退
- 返工视角切换:
  - rework-toggle 开关
  - 新增→需拆除 / 删除→需补装 / 变更→用量调整
  - 按钮切换: 导出Excel / 导出返工变更单
- 筛选面板简化: 移除层级/规格/其他筛选
- Checkbox勾选父件自动传播到所有子件(同一深度区间)

**app.css**
- 新增树形样式: .tree-toggle(expand/collapse/void) .tree-row[data-depth]
- .comp-panels-grid 从 column 改为 row(左右并排)，小屏时 column

### 提交
- `78dc1a7` feat: 比对引擎重构 — 精确匹配 + 树形展示 + 返工变更单 ✅ 已推送
  - 7 files, +889/-386 行

## 生成完整XMind逻辑图

### 需求
将 P1C85Q7HXX8TB56000.xmind 中所有「物料清单」空节点，从数据库补充完整叶子物料，生成新文档。

### 实现
- 创建 `generate_full_xmind.py` 脚本
  - 读取原始XMind (zip) → 解析 content.json
  - 遍历所有节点，正则提取父件PN（`^([A-Z]\d{6}-\d{6}-\d{2,3}[A-Za-z0-9]*)`）
  - 查询数据库获取叶子物料（排除同时也是组件PN的part_number）
  - 为每个「物料清单」节点创建子节点（title=PN+名称，notes=完整规格+用量+单位）
  - 原子写入新XMind文件

### 结果
- 输出: `ogic block diagram/P1C85Q7HXX8TB56000_full.xmind` (356,472 bytes)
- 31/31 个「物料清单」节点全部填充 ✅
- 669 个叶子物料节点成功创建 ✅

## 紧凑版XMind：物料信息合并到父节点

### 需求
删除「物料清单」独立子节点，将物料信息直接写入父组件 nodes 节点作为属性。

### 脚本修改
- `fill_bom_topics()` → `merge_materials_to_parent()` — 向上合并而非向下扩展
- `create_material_topic()` → `format_material_notes()` — 格式化 notes 字符串
- 新增 `_append_notes()` — 安全追加 notes
- 输出后缀 `_full` → `_compact`

### 结果
- 输出: `ogic block diagram/P1C85Q7HXX8TB56000_compact.xmind` (312.3 KB)
- 合并物料: 669 ✅
- Notes格式: 【物料清单】共N个物料 + PN 名称 | qty unit

## 生成 P1C85A7HPX8T701000 逻辑框图

### 需求
按照 compact 版同样逻辑（无独立物料清单节点，notes合并），为 85A7HP 生成逻辑图。

### 实现
- 创建 `generate_xmind_from_db.py` — 从数据库零生成 XMind
  - 从 85Q7H xmind 读取模板（theme/style/结构）
  - 递归构建组件节点树（组件→子组件，叶子→notes）
  - 自动分类：part_number∈parent_pn_set→子组件，否则→叶子物料
  - depth≥2 的节点使用 minorTopic 黄色样式（与原始XMind一致）

### 结果
- 输出: `ogic block diagram/P1C85A7HPX8T701000_compact.xmind` (324.9 KB)
- BOM id=2, 根PN=P1C85A7HPX8T701000, 676条记录
- L1组件: 4个（电子BOM/大配管BOM/结构BOM/专利组件）
- 叶子物料: 636个
- 含 notes 节点: 41个
- 残留「物料清单」节点: 0 ✅

## 精简对比规则区域（去除重复选项）

### 修改内容
- 移除「全量对比（所有物料）」radio（勾选全部组件即等效）
- 移除「仅对比成品模块」checkbox（可通过排除物料号实现）
- 默认选中改为「组件及下挂物料」
- `startCompare()`: 去掉 `compareMode !== 'full'` 分支 → 总是收集组件PN
- `startCompare()`: 去掉 `exclude_leaves` 参数
- `onCompareModeChange()`: 简化为空函数（仅2个模式，无面板灰度逻辑）
- 后端保留 `exclude_leaves` 参数（向后兼容，默认False）

## 差异明细增加「仅叶子+直接上层」筛选

### 修改内容
**后端 (differ.py)**:
- 所有 diff 记录新增 `level_a/b`、`parent_pn_a/b` 字段
- added: level_b, parent_pn_b
- removed: level_a, parent_pn_a
- modified: level_a, parent_pn_a, level_b, parent_pn_b

**前端 (compare.html)**:
- 筛选面板新增「仅叶子+直接上层」checkbox
  - 启用后: 仅展示叶子物料差异 + 其直接父组件差异
  - 算法: parent_pn 集合识别组件PN → 叶子=PN不在组件集中 → 保留叶子 + direct parent PNs
- `applyAllFilters()`: 新增 leafOnly 过滤分支
- `resetFilters()`: 同步重置该 checkbox

## 修复 level/parent_pn 持久化缺陷 + 重新比对验证

### 问题发现
用户要求确认 202 项差异结果后，发现 `differ.py` 中虽为 diff_records 添加了 `level_a/b`、`parent_pn_a/b`，但两个遗漏导致前端「仅叶子+直接上层」筛选失效：
1. `comparison_result` 表缺少这4列
2. INSERT 语句未写入这4个字段

### 修复
- `app/models/bom.py`: CREATE TABLE 增加 `level_a/b (INTEGER)`, `parent_pn_a/b (TEXT)`，末尾添加 ALTER TABLE 迁移逻辑
- `app/services/differ.py`: INSERT 语句增加4字段(?占位符 + 参数)
- 数据库表已包含19+4=23列

### 重新比对
- 删除旧 Task #26 → 重新跑 `run_comparison(bom1, bom2, cross_model, include_children, 4组件)`
- 新 Task #27: 202 项 (88 added + 114 removed, 0 modified)
- 完整字段验证: level_a 114/202, level_b 88/202, parent_pn_a 114/202, parent_pn_b 88/202 ✅
- 层级关系正确: L1→root, L2→L1组件, L3-L6→各级父件

### leaf-only 筛选效果验证
- 识别组件 PN 数: 52
- 叶子物料数: 155, 直接上层: 39 (去重后)
- 筛选后显示: 190/202 项 (隐藏12个中间层级非父组件节点)
- 层级分布: L1:4, L2:20, L3:116, L4:17, L5:6, L6:27

## 删除无意义严重度筛选器

### 背景
- `low` 严重度永远不会被 `_classify_severity()` 产生（只返回 high/medium）
- 当前 Task #27 全为 high，筛选器无实际作用
- 分类过于简单，工程价值低

### 清理确认
- 所有 severity 相关代码已在之前会话清理完毕（differ.py/bom.py/compare.py/reporter.py/app.css 均无 severity 痕迹）
- compare.html 残留空 `rowClass` 变量 → 删除并简化 `<tr>` 标签

## 组件层级视图 — 两层结构差异对比表

### 需求
创建两层结构物料差异对比表：
- **第一层 — 组件层**: 列出组件 PN + 名称
- **第二层 — 叶子物料差异层**: `增加/删除 | 物料编号 | 用量 | 物料名称 | 位号`

### 后端实现
**compare.py** — 新增 `/api/hierarchy/<task_id>` 端点:
- 查询所有差异记录，按 component(PN)=parent_pn 分组
- 从 bom_item 表获取组件名称
- 返回 hierarchy 数组（含 component_pn/name/counts/diffs）+ componentPns + orphans

### 前端实现
**compare.html**:
- 标题栏新增「列表/层级」视图切换按钮组 (view-toggle-group)
- 列表视图 (result-list-view): 保留原有 flat table
- 层级视图 (result-hier-view): 新增表格，列头为 类型|物料编号|用量|物料名称|位号
- 组件行可点击展开/折叠 (toggleCompExpand)
- 差异行左侧色条: 新增绿色/删除红色/变更黄色
- 类型标签: 增加(hier-tag.added) 删除(hier-tag.removed) 数量变更(hier-tag.modified)
- 组件行显示差异统计徽章 (added-badge/removed-badge/modified-badge)
- 分页支持 (HIER_PAGE_SIZE=50)
- CSS: .view-toggle-btn .hier-comp-row .hier-diff-row .hier-tag .hier-comp-badge 等样式

### 验证
- `/api/hierarchy/42` 返回 52 components, 202 diffs, 0 orphans ✅
- 组件 PN + 名称 + counts 正确 ✅
- 差异明细: pn/qty/name/ref 字段完整 ✅
- 前端 HTML 所有关键元素渲染验证通过 ✅

## 整机清机更改通知单 — 自动填表工具

### 需求
读取 P1C100H5FP8R713002 vs P1C100P3EM8R713001 的 BOM 比对结果（Task #45，30项差异），自动填入整机清机更改通知单模版。

### 实现

**app/services/change_notice.py** (新增, 525行):
- `generate_change_notice(task_id)`: 生成 Word (.docx) 通知单
  - 打开 整机清机更改通知单.docx 模版
  - 填写表头：机芯=8R713, 机型=100P3EM→100H5F, 阶段=整机清机, 日期=当天
  - 使用原始 XML 构造差异明细表（绕过 merged cell 限制）
  - 差异行色标：新增=绿色(DCFCE7), 删除=红色(FEF2F2), 变更=黄色(FEF9C3)
  - 列：序号|差异类型|物料编码|物料名称|规格|原用量|新用量|位号

- `generate_change_notice_excel(task_id)`: 生成 Excel (.xlsx) 通知单
  - 使用 openpyxl 生成
  - 表头区域（合并单元格）、差异明细表、冻结窗格、自动筛选
  - 同样的色标方案
  - 横向A4页面

**app/routes/compare.py** — 新增 API:
- `/compare/api/change-notice/<task_id>?format=docx|xlsx`

**app/routes/report.py** — 新增下载端点:
- `/report/api/download/<filename>`

**app/templates/compare.html** — 新增按钮:
- "导出 Word 通知单" (蓝色) / "导出 Excel 通知单" (绿色)
- `exportChangeNotice(format)` JS 函数

### 验证
- Word 输出: 16KB, 表格0=表头(已填充), 表格1=30行差异明细 ✅
- Excel 输出: 8.5KB, sheet=整机清机更改通知单, 冻结窗格+筛选器 ✅
- 色标: 新增绿/删除红/变更黄 ✅
- 所有字段: 物料编码/名称/规格/用量/位号 完整 ✅

## 整机清机更改通知单 — 完全重写

### 问题诊断
用户截图反馈：Word 文档「更改内容」区域完全空白，Excel 只有基础数据行无表单格式。
根因：原代码向文档 body 尾部追加新表格，而非填入模版自身的表格内。

### 解决方案
完全重写 `generate_change_notice()`:
1. 放弃「修改模版表格」方案（合并单元格 XML 操作不可靠）
2. **完全重建表格**: `_build_full_table()` 从零构建整个 10 列表格
3. 用 `body.replace(old_tbl_elem, new_tbl)` 替换模版原表格
4. 保留模版 P0/P1 标题段落

表格结构（37 行）:
- R0: 机芯 / 日期（合并单元格）
- R1: 机型 / 订单号
- R2: 阶段 / 数量
- R3: 更改内容 — 蓝色全宽标题（含差异统计）
- R4: diff 列头（8 列分布在 10 列网格中）
- R5-R34: 30 项差异数据行（绿色新增 / 红色删除 / 黄色变更）
- R35: 说明
- R36: 拟制 / 审核 签审行

关键技术:
- 使用 `_oxml()` 构建全部 XML 元素
- `gridSpan` 属性实现水平合并
- `w:shd` 设置单元格背景色
- `w:tcW` 控制列宽

### 同步更新 Excel
- 统一布局: 标题 → 信息栏 → 汇总 → 表头 → 数据 → 签审区
- 冻结窗格 A6，自动筛选 A5:H35
- 色标与 Word 一致

## 整机清机更改通知单 — 第三次重写（文本段落格式）

### 原因
用户提供正确版本截图，指出版面结构偏差：
- 更改内容不是表格网格，而是**文本段落**
- 按父组件分组：`在<编号>……<名称>里` 开头
- ADD:/DEL: 前缀，续行缩进
- 分隔符：物料编号··名称··数量PC
- 左侧竖排"更改内容"标签，右侧大单元格容纳全部段落

### 实现
**group_diffs_by_parent()** 重构：
- 从 bom_item 表批量查询父组件名称（comparison_result 无 parent_name）
- 过滤中间组件（所有子项都是子组件的 PN 则跳过）
- 按首词（split[0]）合并等价组件（如 100H5F.包装组件 + 100P3EM.包装组件）
- 排序：按 short_name 字母序

**_build_full_table()** 完全重写：
- 网格简化为 4 列 (label 1200 + value 3200 × 2)
- 头两行：机··芯/日··期，机··型/订·单·号
- 第三行：左侧 600dxa 竖排"更改内容"(宋体, blue, 24pt)，右侧 span=3 大单元格(7600dxa)
- 段落格式：
  - 组件标题：bold, 22pt, #1E40AF, 左对齐
  - ADD/DEL 首行：20pt, #334155, 缩进 360dxa
  - ADD/DEL 续行：无前缀，同缩进
  - MOD 行：`MOD:<pn>··<name>··<old>→<new>`，同缩进
- 底部："根据送更王此江茵"（span=4）

日期格式：Windows 平台 `%Y/%#m/%#d` → 2026/6/11（无前导零）

模型提取修复：
- `_extract_model()` 正则改为 `^P1C(.+?)(\d+R\d+)(\d*)$`
- 正确提取 100P3EM / 100H5FP

Excel 同步修复：从新 diff_rows 格式（无 spec/ref/bg/fg 字段）适配

## 修复 BOM 比对方向（source/target 对调）

### 问题
Task 45 的 source=100P3EM, target=100H5F 搞反了。
- ADD 实际是 H5F 独有物料，DEL 是 P3EM 独有物料
- 但业务逻辑应是：现有 H5F 物料 → 改成生产 P3EM

### 用户确认的业务方向
- 手上现有：100H5F 物料
- 目标产品：100P3EM
- ADD = 需要新采购的物料（P3EM 有、H5F 没有）
- DEL = 需要剔除的旧物料（H5F 有、P3EM 没有）

### 修复（直接 SQL 翻转）
1. UPDATE comparison_task: source_bom_id 3↔4, task_name
2. 逐行 UPDATE comparison_result: diff_type added↔removed 互换
3. 逐行交换所有 _a/_b 列（part_number, part_name, reference, quantity, level, parent_pn）

### 结果
- 翻转前: added=14, removed=15, modified=1
- 翻转后: added=15, removed=14, modified=1 ✅
- 机型: 100P3EM ✅
- ADD 项: P3EM 新增物料（遥控器 YK-8E00, BEZEL 100P3E, FFC 96pin 等）✅
- DEL 项: H5F 旧物料（遥控器 YK-8H00, BEZEL 100H5F 等）✅
- task_name: 100H5F vs 100P3EM ✅
- Word + Excel 重新生成并验证通过 ✅

## 物料名称清洗（clean_material_name）

### 新增函数
`app/services/change_notice.py` → `clean_material_name(name)` (~70行)

### 清洗规则（按顺序）
1. 去除括号内容（替换为空格，避免 Pro(Y)K8H → ProYK8H 粘连）
2. 精确匹配去除噪声词：RoHS, REACH, CBU, SKYWORTH, coocaa, COOCAA, HKC, WANZHONG, 内销, 通用, 中国, 无, 非生产打印, 彩色印刷, CARTONBOX, NEW, EMMC, 64G, Shenzhen, CHUANGWEI-RGB, Electro, 深圳创维RGB电子, High-Performance, 陶瓷基板, 双面, 同面, 非高频膜, 2Point, 80g, 8R713, 8R710, NMN, L1, A, B, C, BC
3. 模式匹配去除：\d+mm, \d+\.\d+mm, 温度范围, N按6-6-3格式编码, WZ.内部编码, N11+数字编码, 下划线开头的token, 含下划线的全大写内部编码（不含破折号避免误杀型号 YK-8E00-00）
4. 去除末尾状态码：0, 1, 2, 3, A, B, C, BC （包括 0. 1. 等）
5. 去除 8R713-/8R710- 前缀（如 8R713-100P5FP → 100P5FP）

### 应用范围
- `get_diff_rows()`: nm（物料名称）、parent_lookup（组件名称）均经清洗
- Word `_add_items()`：清洗后的 nm 直接用于段落生成
- Excel 获取清洗后的 nm 来自 `get_diff_rows()`

### 验证
零残留：无 RoHS, REACH, SKYWORTH, coocaa, 中国, 内销, 通用 等噪声词
遥控器 YK-8E00-00 ✅ (型号保留)
BEZEL 100P3E MAX ✅ (WANZHONG/WZ代码清除)
普通FFC 96pin ✅ (0.5mm/温度/REACH/A/0.清除)
贴片电阻 33Ω 1/4W ±5% ✅ (1206/温度/陶瓷基板清除)
快速操作指南 100H5F Pro YK8H ✅ (括号替换为空格，Pro YK8H不再粘连)
