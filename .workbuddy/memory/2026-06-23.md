## Ubuntu 26.04 LTS 部署包制作 (09:05)

**触发**：用户要求梳理技术栈并打包项目用于 Ubuntu 服务器部署。

**技术栈梳理**：
- Flask 3.1.1 + Gunicorn + Nginx + SQLite
- 依赖：Flask, Flask-Compress, openpyxl, xlrd, pandas, python-docx, rapidfuzz, Werkzeug

**产出**：
1. `wsgi.py` — Gunicorn 生产入口
2. `deploy.sh` — 一键部署脚本（系统依赖→虚拟环境→systemd→Nginx 全自动）
3. `deploy.md` — 完整部署文档（含手动步骤、故障排查）
4. `bom-comparison-deploy-20260623.tar.gz` (1.4MB) — 部署压缩包
5. 修复 `change_notice.py` 重复常量定义 + Linux 字体回退

**压缩包排除**：venv/、__pycache__/、.git/、*.db、*.log、*.bat、测试数据 Excel 等

## 自动更新系统开发 (09:48)

**触发**：用户要求开发一键式持久化维护脚本，支持环境检测/自动备份/更新安装/验证/回滚/日志/定时调度。

**产出**：
1. `auto_update.sh` (1800+ 行) — 主控脚本
   - 命令：check / update / rollback / status / backup / schedule / help
   - 模式：--silent / --force / --dry-run
   - 7 步更新流程：检测→备份→停服→更新→依赖→启服→验证
2. `update_config.conf` — 配置文件（package/git/http 三种更新源）
3. 守护进程模式 daemon/stop-daemon（crontab 不可用的替代方案）

**服务器适配**：
- 发现 crontab 不可用 → 增加 daemon 模式
- 发现多用户 gunicorn 误匹配 → 按端口(40045)精确过滤进程
- 修复 bash `local var=$?` 陷阱（local 命令重置 $?）
- 修复 `set -e` + 通配符在空目录时非预期退出

**测试验证**：
- ✅ 环境检测（Python/磁盘/DB/端口/进程）
- ✅ 备份创建（DB + 代码 + 配置，保留策略）
- ✅ 包验证（缺少 code/ 目录的无效包被正确拒绝）
- ✅ 进程检测（精确匹配端口 40045）
- ✅ 守护进程 start/stop 循环
- ✅ dry-run 完整流程
- 已 commit + push

## compare 界面全选/单选框联动 Bug 修复 (10:08)

**触发**：用户报告：全选取消勾选后，点击单个组件复选框错误触发全选（所有组件被选中）。

**根因分析（3 个缺陷）**：

| # | 缺陷 | 严重度 | 说明 |
|---|------|--------|------|
| 1 | **级联循环** | 🔴 严重 | `onCompCheckChange` 直接设置 `comp-toggle.checked = allChecked`。若 `allChecked=true`（如单根节点树），且浏览器在程序化赋值时派发 `change` 事件，则触发 `toggleAllComponents` → 所有复选框被勾选 |
| 2 | **状态丢失** | 🔴 严重 | `renderComponentTree` 硬编码 `checked` 属性。展开/折叠/搜索/过滤触发树重绘时，用户手动取消的勾选全部被重置为勾选状态 |
| 3 | **隐藏行遗漏** | 🟡 中等 | `toggleAllComponents` 只操作 `display !== 'none'` 的可见行。折叠节点的子项复选框不受全选/取消全选影响，展开后状态不一致 |

**修复方案（compare.html）**：
1. 新增 `_compToggleGuard` 守卫标志 → 阻断 `onCompCheckChange → toggle-all update → toggleAllComponents` 级联链
2. 新增 `compCheckedState = {source: {}, target: {}}` 缓存层 → `renderComponentTree` 重绘时读取缓存恢复状态
3. 新增 `_syncCompCheckState(side)` → 将 DOM 中所有 checkbox 状态同步到缓存
4. `loadComponents` 初始化时预填充缓存（所有有子件的节点默认 true）
5. `renderComponentTree` 渲染时用 `(pn in cache) ? cache[pn] : true` 替代硬编码 `checked`
6. `toggleAllComponents` 移除可见性过滤 → 操作全部 checkbox 并同步缓存

**部署**：已上传到 172.20.217.12:40045，服务已重启确认生效（PID 1867277）。
**提交**：commit 3a7ffb7，已 push 到 GitHub。
**固化**：用户确认问题已解决，代码已固化。无积压未提交变更。

## compare 单选对比交互逻辑验证 (10:22)

**触发**：用户要求验证单选组件是否能完成差异对比。

**验证结果**：
- ✅ 交互逻辑正确，单选一个组件即可完成对比（非 Bug）
- checkbox 仅赋能于父组件（`hasKids=true`），叶子节点无 checkbox
- `selected_components` 为空时不触发过滤 = 全量对比；非空时按 PN 过滤
- `components_only` 模式：过滤到选中组件本身 + 其直接子件
- `include_children` 模式：递归收集选中组件的完整子树
- `skip_pns` 作为独立的反向排除机制，与 `selected_components` 正交
- 3 种场景（全选/多选/单选）均已验证逻辑一致

## compare 组件列表排序规则确认 (10:26)

**触发**：用户要求确认组件列表排序规则及一致性。

**结论**：按 SAP BOM 原始行号 `line_no` 固定排序，非随机。

**排序链**：
1. SAP BOM 解析器 (`parser.py:580`) 保留原始行号写入 `bom_item.line_no`
2. 接口 `GET /upload/api/bom-tree/<id>` (`upload.py:138-139`) 按 `ORDER BY line_no` 查询
3. 前端 `renderComponentTree()` 原样遍历 JSON 数组，**无客户端排序**
4. 同层子节点按 `line_no` 升序，树结构递归保持一致

**特征**：固定排序 / 不可配置 / 不按名称或 PN 排序 / 数据加载后始终一致

## compare 排序一致性深度排查 (10:30)

**触发**：用户报告排序与实际组件排序不一致。

**排查方法**：完整追溯 6 层排序链 + 服务器实际数据验证。

**全链路验证**：

| 层 | 位置 | 排序依据 | 验证方式 |
|----|------|---------|---------|
| ① SAP 文件 | `clean_bom_data()` L405-428 | 逐行读取，原序 `append` | 代码审查：无排序/分组/打乱 |
| ② line_no 赋值 | `parser.py` L445 | `idx + 1`（枚举序） | 代码审查：`enumerate()` 保序 |
| ③ SQL 查询 | `upload.py` L139 | `ORDER BY line_no` | 服务器实测：100H5F/100P3EM 各 500 行验证 |
| ④ children_map | `upload.py` L147-152 | Python 3.7+ dict 插入序 | `setdefault()` 追加，保序 |
| ⑤ build_node 递归 | `upload.py` L160-171 | 子节点按 dict 中的序 DFS | 服务器实测：L1→L2 链路完整一致 |
| ⑥ 前端渲染 | `compare.html` L659 | `for (var i=0; i<nodes.length; i++)` | 代码审查：无排序/无 shuffle |

**结论**：**排序逻辑完全一致，不存在 Bug**。排序规则 = SAP BOM 原始文件中各组件的出现顺序（section 顺序），这是一个确定性规则，不是随机排序。

**两个 BOM 的 L1 顺序差异（验证数据）**：
- 100P3EM：大配管→电子→结构→专利（SAP 文件顺序）
- 100H5F：电子→大配管→结构→专利（SAP 文件顺序）
- 两个文件 section 顺序不同 → 显示排序不同，但各自由各自的 SAP 文件决定

## 修复 BOM 对比结果排序错乱 (10:39)

**触发**：用户报告对比结果中组件排序与原始 BOM 不一致。

**根因**（2 处缺陷）：
1. `comparison_result` 表**无 `line_no` 列** — 原始排序信息从根源丢失
2. `get_diff_rows()` SQL: `ORDER BY diff_type, COALESCE(parent_pn_a, parent_pn_b)` — 按**父件 PN 字母序**排序，非 line_no
3. `group_diffs_by_parent()`: `sorted(final_groups, key=lambda g: g['parent_pn'])` — **再次**按字母序排序

**修复**（6 文件，45 行）：
- `bom.py` / `__init__.py`: 新增 `line_no_a`/`line_no_b` 列 + 迁移
- `differ.py`: 所有 diff_record 携带 line_no 入库
- `compare.py` / `reporter.py`: 查询按 `COALESCE(line_no_b, line_no_a)` 排序
- `change_notice.py `: 
  - `get_diff_rows()`: 排序键改为 line_no
  - `row_data` 新增加 `line_no` 字段
  - `group_diffs_by_parent()`: 用 `min_line_no` 排序替代字母序
**部署**：已上传到 172.20.217.12:40045，服务已重启（PID 1907330）
**提交**：commit 5788aba，已 push 到 GitHub。

## 组件列表跨BOM排序对齐 (10:58)

**触发**：用户发现基准和目标BOM的根级组件排列顺序不一致（100H5F: 电子→大配管→结构→专利，100P3EM: 大配管→电子→结构→专利）。

**根因**：SAP 源数据中两个 BOM 的 line_no 分配不同，系统如实按 line_no 排序，导致跨BOM对比时组件不对齐。

**修复方案**：按名称关键词排序（方案B），不依赖 SAP line_no，也不依赖 PN 字母序。
- 排序规则：电子(1) → 大配管/大配置(2) → 结构(3) → 专利(4) → 其他(99)
- 只影响根级组件排序，子件保持 SAP 原始 line_no 顺序不变

**修改**：`upload.py` get_bom_tree() 新增 root_items.sort(key=_category_key)（+10 行）

**部署**：SCP 到 172.20.217.12:40045，已重启（PID 1933579）
**提交**：commit 1785fa1，已 push 到 GitHub。

## 缓存修复：BOM树API添加no-cache头 (11:08)

**触发**：用户截图验证发现排序未更新，排查确认为浏览器缓存导致。
**修复**：`upload.py` get_bom_tree() 返回添加 `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma: no-cache` + `Expires: 0`，强制浏览器每次重新拉取。
**部署**：已部署，PID 1942850。验证 curl -I 确认缓存头生效。
**提交**：commit 859574c，已 push。

## 根因修复：sortTreeBySource 覆盖了服务端排序 (11:09)

**触发**：用户第三次截图验证发现排序仍未对齐。进一步排查发现缓存头不是根因。
**真正根因**：`compare.html` `renderComponentTree()` L704-706 对目标BOM调用 `sortTreeBySource()`，内部 L864-867 的 globalOrder 按PN匹配排序。根节点无父件→orderMap/nameOrderMap为空→触达step4 globalOrder→目标BOM 4个PN只有1个（N120103-000127-000专利组件）匹配源BOM PN→专利得分3、其余99999→专利排第一，服务端的类别排序被覆盖。
**修复**：`sortTreeBySource` 的 nodes.sort 中新增 step4「名称类别排序」（电子→大配管→结构→专利），作为 globalOrder 之前的优先级，与服务端 `_category_key` 一致。step4 改为 step4+step5。
**部署**：已部署，PID 1946896。
**提交**：commit bab0718，已 push。

## 比对类型下拉改按钮组 (11:24)

**触发**：用户要求将比对类型从下拉选择改为点选按钮组。
**修改**：`compare.html` 中 `<select id="compare-type">` 替换为 `<input type="hidden" id="compare-type" value="version">` + 两个并排按钮（版本比对/跨机型比对）。CSS: 未选中白底灰字灰边 hover→深灰；active 蓝色填充(#2563eb)白字 hover→深蓝(#1d4ed8)。新增 `onCompareTypeChange(btn)` 切换 hidden input value + toggle active class。向后兼容：JS 中 `document.getElementById('compare-type').value` 不变。
**部署**：已部署，PID 1966510。
**提交**：commit 298ee24，已 push。

## 组件/叶子/物料三级分类 (11:36)

**触发**：用户要求将组件和叶子数据从"物料"分类中独立出来。
**修改**：
- `differ.py`：新增 `parent_pns` 集合 + `_get_category(pn)` 函数，判断 PN 是否有子件 → component/leaf
- `compare.html`：CAT_MAP 新增 `component: '组件', leaf: '叶子'`，筛选图例文字同步
- `reporter.py`：DIFF_CATEGORY_LABELS 新增对应中文标签
**部署**：已部署，PID 1977133（但 compare.html 路径写错未生效⚠️）
**提交**：commit 42220f0 + 后补部署 → commit f023686，已 push。

## 修复分类显示英文 + 叶子→物料重命名 (11:42)

**根因**：上次部署 scp 目标路径写错（services/ → templates/），compare.html 未更新。
**修复**：重新 SCP + 将 `leaf: '叶子'` 改为 `leaf: '物料'`（用户确认叶子=物料）。
**部署**：已部署，PID 1985546。
**提交**：commit f023686，已 push。

## 修复点击表格头排序损坏 (11:49)

**触发**：用户点击比对结果表格的列头（红色框标注区域），排序后显示异常。
**根因**：`app.js` `initTableSort()` L354 将 dataArray 参数直接用 `[].concat(dataArray)` 当数组处理。但 compare.html 传入的是函数 `dataGetter()`，导致 `[].concat(fn)` → `[fn]`，排序逻辑完全失效。
**修复**：`app.js` L353-354：先判断 `typeof dataArray === 'function'` → `dataArray()` 取值，再 concat 排序。
**部署**：已部署，PID 1995130。
**提交**：commit 29b3c7e，已 push。