# 2026-07-03 工作日志

## 服务健康检查 + 积压清理

### 健康检查
- HTTP 首页 / 比对页面均正常 200
- gunicorn 4 workers 全部在线 (PID 680971, 6/27 至今)
- 6/29 有 2 worker 被 OOM kill，已自动恢复，无后遗症

### 代码清理提交 (85195c8)
- `debug_export.py` / `generate_full_xmind.py` / `generate_xmind_from_db.py` / `verify_docx.py`: 硬编码绝对路径 → `os.path` 相对路径
- `MEMORY.md`: 补充位号变更检测相关技术决策
- `SOP_BOM核对操作指南.md`: 删除（已过时）
- 新增 6/25 和 6/27 工作日志
- `.codebuddy/` 和 `bom-comparison-deploy-*.tar.gz` 保持未跟踪

### refactor: upload 页面移除批量导出对比卡片 (fa7eac7)
- 删除 dual-export-card（基准/目标 BOM 下拉框 + 导出按钮），工作中用不到
- 删除 exportDualBoms() 函数和 loadBomSidebar 中的下拉框填充逻辑
- 后端 `/upload/api/export-dual` 路由保留（bom_list.html 仍在用）

### 部署到服务器 (17:50)
- stop → scp 同步代码 → run.sh --force → health_check
- 旧 PID 680971 → 新 PID 826197，HTTP 200 验证通过
- upload 页面验证：批量导出对比卡片已消失
- 健康检查 5/7 通过（重启日志 + 时间戳差异属正常）

### refactor: upload 页面删除已上传BOM侧边栏 (deb784f)
- 删除侧边栏卡片（grid 3列→全宽），loadBomSidebar() / exportBom() 函数
- upload 区撑满全宽，BOM 列表管理由 bom_list 页面负责
- 二次部署 (17:55): PID 826197 → 837212，验证通过
