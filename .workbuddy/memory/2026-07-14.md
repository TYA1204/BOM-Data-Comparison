# 2026-07-14

## 服务挂掉修复 + 看门狗部署

**问题**：http://172.20.217.12:40045/ 打不开，检查发现 gunicorn 进程已死，且 pycache 残留堵死启动链路。

**修复**：
1. `stop.sh` 清缓存 → `run.sh` 重新启动，服务恢复（HTTP 200）

**根因对策**（两个层面）：

### 1. run.sh 增加 AUTO_RECOVER 环境变量
- 当 `AUTO_RECOVER=true` 时，遇到 pycache 残留自动清除而不是阻止启动
- 确保自动恢复链路不被 pycache 堵死
- 文件：`scripts/run.sh`（第 20-24 行新增检测逻辑）

### 2. 新增 watchdog.sh 看门狗守护脚本
- 每 30 秒 curl 检查端口 40045 是否存活
- 连续两次失败（60s）才触发恢复，避免偶发抖动误触
- 恢复流程：stop.sh（清缓存）→ AUTO_RECOVER=true run.sh（重启）
- 支持 start/stop/status/restart 命令
- PID 文件：watchdog.pid，日志：logs/watchdog.log
- 已部署到服务器，状态正常

**关键变更**：
- `scripts/run.sh` — 新增 AUTO_RECOVER 环境变量支持
- `scripts/watchdog.sh` — 新建看门狗脚本

## BOM 列表删除"组件/逻辑图/XMind"功能

**原因**：实际工作中未使用到这3个功能（红色框中的按钮），决定移除。

**改动范围**：
- `app/templates/bom_list.html`：删除"组件""逻辑图""XMind"3个按钮、对应JS函数（exportAllComponents/openLogicDiagram/quickExportXmind等）、两个Modal（logic-diagram-overlay/xmind-export-overlay）、SVG树构建代码、XMind导出代码、拖拽平移缩放代码（1053行→409行）
- `app/routes/upload.py`：删除3个路由（/api/export-all-components、/api/export-xmind、/api/export-xmind-batch）、3个helper（_get_db_conn、_get_xmind_template_path）、xmind_export导入、io/sqlite3导入
- `app/services/xmind_export.py`：删除
- `generate_full_xmind.py`、`generate_xmind_from_db.py`：删除（独立脚本）
- `ogic block diagram/`：删除（XMind模板目录）
- `app/static/css/app.css`：删除 logic-diagram 段、indigo text utility
- `docs/BOM_Skills_使用手册.html`：更新两处表格，反映新按钮列表

**保留**（关键！）：
- `/api/components/<bom_id>` 路由 — 上传页"按组件导出"卡片用
- `/api/bom-tree/<bom_id>` 路由 — 比对页组件选择面板用
- `/api/export-components` 路由 — 上传页"按组件导出"卡片用
- `reporter.py` 中 `generate_components_export_excel()` 函数 — 上传页用

**验证**：
- 7项健康检查全部通过（exit 0）
- `/api/export-xmind/1` → 404（已删）
- `/api/export-all-components/1` → 404（已删）
- `/api/components/1` → 200（保留）
- `/api/bom-tree/1` → 200（保留）
- 比对页功能未受影响
